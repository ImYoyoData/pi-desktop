/** LAN web stub — skip mermaid/diagram bundle on phones. */
export function resetMermaidForTheme(_dark?: boolean): void {
  /* no-op */
}

export async function mountMermaidIn(
  _root?: ParentNode | null,
  _dark?: boolean,
  _labels?: unknown,
): Promise<void> {
  /* no-op */
}
